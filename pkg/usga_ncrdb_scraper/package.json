{
  "name": "usga_ncrdb_scraper",
  "version": "0.0.1",
  "private": true,
  "description": "Standalone CLI that scrapes the USGA National Course Rating Database (ncrdb.usga.org) into JSONL, and syncs it into the app's read-only usga_* tables.",
  "type": "module",
  "scripts": {
    "scrape": "bun ncrdb.ts scrape",
    "search": "bun ncrdb.ts search",
    "describe": "bun ncrdb.ts describe",
    "test": "bun test",
    "sync:local": "nu scripts/sync.nu --local",
    "sync:remote": "nu scripts/sync.nu --remote"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}
